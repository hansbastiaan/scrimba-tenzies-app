/**
 * Challenge: Start a brand new React app! +> Done
 * - Create a separate App component => Done
 * - Import and render the App component here => Done
 * - In the App component, render a <main> element => Done
 * - Style everything to look like the slide
 */

import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./styles.css";

ReactDOM.render(<App />, document.getElementById("root"));
